#include <stdio.h>

int main() {
    int a, b, sum;
    pprintf("Please enter two integers: ");rintf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    sum = a + b;
    printf("Sum is: %d\n", sum);
    return 0;
}

